{{! aqui o usuário terá acesso a lista e poderá pesquisar de acordo com o nome do médico ou especialidade }}
{{#if botaoDashboard}}
<div class="d-flex justify-content-end mt-3">
  <a href='/dashboard' class='btn btn-outline-secondary px-5 mx-2 '>Dashboard</a>
</div>
{{else}}
{{/if}}


<h1 class='text-start m-5'>Bem Vindo, {{nomeAcesso}}</h1>

<h2 class='text-center m-5'>Médicos e Especialidades</h2>
<header class='d-flex mb-2'>
  <form action='/search/doctorUser' method='get' class='input-group d-flex justify-content-center'>
    <input
      type='text'
      name='searchDoctor'
      placeholder='nome do médico'
      class='input-group-text'
    />
    <button
      type='submit'
      class='btn btn-primary bi bi-search input-group-text'
    ></button>
  </form>
  <form action='/search/specialtyUser' method='get' class='input-group d-flex justify-content-center'>
    <input
      type='text'
      name='searchSpecialty'
      placeholder='especialidade'
      class='input-group-text'
    />
    <button
      type='submit'
      class='btn btn-primary bi bi-search input-group-text'
    ></button>
  </form>
</header>
<section class='container d-flex flex-wrap m-5'>
  {{!-- <table class='table table-hover' id='tabela'>
    <thead>

      <th>CRM</th>
      <th>Nome</th>
      <th>Email</th>
      <th>Atendimento</th>
      <th>Especialidade</th>

    </thead>
    <tbody id='tbody'> --}}

      {{#each resultadoAtivos as |ativo|}}
        {{!-- <tr>
          <td>{{ativo.medico.crm}}</td>
          <td>Dr(a). {{ativo.medico.nome}}</td>
          <td>{{ativo.medico.email}}</td>
          <td>{{ativo.medico.atendimento}}</td>
          <td>{{ativo.especialidade}}</td>
        </tr> --}}
 
      <div class="card text-center m-2" style="width: 18rem;">
        <div class="card-header bg-primary text-white" style="height: 4rem;">Dr(a). {{ativo.medico.nome}}</div>
        <ul class="list-group list-group-flush">
          <li class="list-group-item bg-primary bg-opacity-75 text-white">CRM {{ativo.medico.crm}}</li>
          <li class="list-group-item bg-primary bg-opacity-75 text-white" style="height: 5rem;">{{ativo.especialidade}}</li>
          <li class="list-group-item text-lowercase bg-primary bg-opacity-75 text-white">{{ativo.medico.email}}</li>
          <li class="list-group-item bg-primary bg-opacity-75 text-white">{{ativo.medico.atendimento}}</li>
        </ul>
      </div>

      {{/each}}

    {{!-- </tbody>
  </table> --}}
</section>
{{>footer}}
{{! Fim da lista de Ativos }}